/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["leerob.io", "upload.wikimedia.org", 'm.media-amazon.com', 'i0.wp.com', 'media.licdn.com']
    }
}

module.exports = nextConfig
